std = "roblox"

[lints]
# UIManager and InputHandler build every UDim2 with UDim2.new for readability;
# rewriting them to fromScale/fromOffset is cosmetic churn, so this lint is off.
roblox_manual_fromscale_or_fromoffset = "allow"
